"""Database seed script placeholder.

No domain models exist yet in Phase-0 (no users/accounts/transactions), so
there is nothing to seed. Real seeding logic starts in F1/F2 once those
models land - this stub exists so the `scripts/` entrypoint and its
invocation pattern (e.g. `python -m scripts.seed` or via a Docker/CI step)
are already wired up.
"""


def main() -> None:
    print("no seed data yet")


if __name__ == "__main__":
    main()
